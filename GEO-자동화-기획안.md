# GEO 자동화 서비스 기획안
### "AI 검색 노출(GEO)" 진단부터 수주·견적·실행·리포트까지 — 사람 최소 개입 파이프라인

> 한 줄 요약: 의뢰인이 ChatGPT·Perplexity에서 변호사/병원을 찾는 시대에, **"우리가 AI 답변에 나오는지"를 자동 측정→영업→계약→매월 추적**까지 무인에 가깝게 돌리는 시스템.

---

## 0. 전제와 솔직한 한계 (먼저 읽을 것)

완전 무인 자동화는 현실적으로 **"사람 개입 최소화"**가 정확한 표현입니다. 두 곳은 사람 게이트를 남기는 걸 권장합니다.

- **LLM 응답의 비결정성**: 같은 질문도 매번 답이 다릅니다. → 측정은 *질문당 N회 × 다엔진* 반복 후 **노출 빈도(%)**로 환산해야 신뢰도가 생깁니다. 이게 이 사업의 핵심 기술 난점이자 해자(垓子)입니다.
- **컴플라이언스(변호사·의료 광고규정)**: "노출 보장" 같은 표현 금지. 제품은 *측정·진단 도구*로 포지셔닝하고, 계약/문구는 사람이 최종 검수.
- **콜드 아웃리치 합법성**: 이메일/문자 발송은 정보통신망법(수신동의)·개인정보 이슈가 있어, 공개정보 기반 B2B 영업 범위와 옵트아웃을 설계에 반영.
- **API 비용**: 진단 1건 = 수십~수백 LLM 호출. 비용·캐싱·레이트리밋 관리가 마진을 좌우.

---

## 1. 전체 파이프라인 (4단계)

```
[1.마케팅/리드]  →  [2.수주]  →  [3.견적]  →  [4.실행·결과치]
 자가진단 미끼        상담예약       자동 견적엔진     월간 자동추적
 콜드 자동진단        자동 제안서     점유율·경쟁강도    자동 리포트 발송
 자가 GEO 노출        전자계약       기반 가격 산정     콘텐츠 처방 생성
```

### 1단계 · 마케팅 / 리드 생성 (수요 창출)
- **인바운드 미끼**: 자가진단 웹툴(`lawgeo.html`, *현재 완성*) → 변호사가 직접 점유율을 보고 충격 → 사전등록.
- **아웃바운드 자동진단**: 타깃 로펌 리스트 수집(공개정보) → 각 로펌을 **자동 진단(백엔드가 AI 호출)** → 개인화 진단 리포트 자동 생성 → "경쟁사 A·B는 나오는데 귀사는 안 나옵니다" 메일/문자 자동 발송.
- **자가증명 콘텐츠**: 우리 콘텐츠를 AI가 "한국 로펌 AI 검색 최적화"에 인용하게 만들기 → 제품 작동 증명 + 인바운드.

### 2단계 · 수주 (리드 → 계약)
- 리드가 리포트 보고 **상담 예약**(Cal.com 임베드, 자동) → 진단 데이터 기반 **제안서 자동 생성**(PDF) → **전자계약**(모두싸인) → 구독 결제(토스페이먼츠/Stripe).

### 3단계 · 견적 (자동 가격 산정)
- 입력: 분야 경쟁강도 + 현재 점유율 + 지역 + 목표.
- 규칙 기반 + AI 보정 견적 엔진 → 진입(진단 무료) / 월 추적 구독 / 콘텐츠 최적화 옵션 3단 자동 산출.

### 4단계 · 실행 · 결과치 (delivery / 리포트)
- **월간 스케줄러(cron)**가 동일 질문 세트를 재질의 → 점유율 시계열 갱신 → **대시보드 + 자동 PDF 리포트 발송**.
- **콘텐츠 처방 자동 생성**: AI가 GEO 최적화 FAQ·전문분야 페이지·스키마(JSON-LD) 초안 작성 → 고객이 그대로 게시.
- GA4/검색유입 연동으로 실제 성과 측정.

---

## 2. 시스템 아키텍처

| 레이어 | 구성 | 후보 스택 |
|---|---|---|
| 프런트 | 랜딩 + 자가진단 + 고객 대시보드 | 정적 HTML(현재) → Next.js |
| 백엔드 | 진단 잡, 견적, 리포트 API | 서버리스(Vercel/Cloudflare Workers) |
| 스케줄러 | 월간 재진단 큐 | Cron + 큐(Upstash/QStash) |
| **AI 측정**(외부) | buyer-intent 프롬프트를 *실제 소비자 엔진*에 배치 실행 | Gemini API(무료 티어)·Perplexity·OpenAI |
| **AI 처리**(로컬) | 응답 판독(언급 분류)·콘텐츠 처방 생성·아웃리치 개인화 | 로컬 Gemma 3n E2B/E4B (자체 서버) |
| 데이터 | 리드·진단이력·점유율 시계열 | Supabase / Postgres |
| 아웃리치 | 이메일·문자·예약 | Resend·솔라피·Cal.com |
| 결제·계약 | 구독·전자계약 | 토스페이먼츠·모두싸인 |
| 리포트 | 자동 PDF 생성·발송 | 서버 렌더 → 메일 |

**측정 엔진(핵심)의 동작**
1. 분야×지역으로 질문 세트 생성(현재 `lawgeo.html`의 템플릿 로직 재사용).
2. 각 질문을 **실제 소비자 엔진**(Gemini·Perplexity 등)에 N회 호출.
3. 응답 텍스트에서 대상 브랜드/경쟁사 언급을 **로컬 Gemma**가 판독(분류).
4. **노출률 % + 경쟁사 대비 순위 + 시계열**로 집계.

> ⚠️ **측정과 처리를 반드시 분리.** 측정(질문)은 *실제 ChatGPT/Perplexity/Gemini*를 거쳐야 한다 — 로컬 모델에 물으면 "그 모델 의견"일 뿐 고객이 노출되고 싶은 엔진의 답이 아니므로 *다른 걸 재는* 셈. 반대로 판독·콘텐츠 생성·영업문구 같은 **처리**는 로컬 Gemma E2B/E4B로 무한정 공짜 처리 → 외부 API 비용은 '측정 쿼리'라는 최소 조각만 남는다.

---

## 3. 단계별 로드맵 (MVP → 풀 자동)

| Phase | 산출물 | 사람 개입 | 상태 |
|---|---|---|---|
| **0** | 반자동 자가진단 툴(`lawgeo.html`) | 직접 AI에 붙여넣기 | ✅ 완료 |
| **1** | **진단 자동화** 백엔드(AI 호출→점유율 산출) + 리드 DB + 사전등록 실연결 | 거의 없음 | 다음 |
| **2** | 콜드 아웃리치 자동화(리스트→자동진단→개인화 발송) + 예약/제안서 | 발송 승인 | |
| **3** | 견적 엔진 + 전자계약 + 구독결제 | 계약 검수 | |
| **4** | 월간 추적 스케줄러 + 자동 리포트 + 콘텐츠 처방 = 풀 파이프라인 | 품질·컴플 검수 | |

→ **Phase 1만 완성돼도** "버튼 한 번에 임의 로펌 점유율 리포트 자동 생성"이라, 영업 생산성이 수십 배가 됩니다.

---

## 4. 수익 모델 / 유닛 이코노믹스 (가설)

- **진단**: 무료(깔때기 입구).
- **월 추적 구독**: 로펌 9~30만 원/월.
- **콘텐츠 최적화 옵션**: 건/월 추가.
- 변호사 수임료가 건당 수백만 원 → "월 30만 원으로 의뢰인 1명만 더 들어와도 본전" 논리로 클로징.
- 비용 핵심 = **측정 쿼리뿐**. 판독·생성·영업문구는 로컬 Gemma로 0원. 측정도 Gemini 무료 티어 + 질문셋 공유 캐싱으로 진단 1건 원가 수백 원대 → 구독 마진 90%+ 목표.

**핵심 KPI**: 무료진단→상담 전환율 · 상담→구독 전환율 · 월 이탈률 · 진단 1건 원가 · 점유율 개선 실측치(성과 증명 = 리텐션).

---

## 5. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| LLM 비결정성으로 측정 신뢰도 흔들림 | N회·다엔진 평균, 신뢰구간 표기 |
| 광고규정 위반 | "측정·진단"으로 포지셔닝, 보장표현 금지, 사람 검수 |
| 콜드 아웃리치 규제 | 공개 B2B 정보 한정, 옵트아웃, 발송 동의 설계 |
| 대형 SEO사·GEO 대행사 진입 | 버티컬 특화(로펌) + 셀프서비스 가격으로 차별화 |
| API 단가 상승/모델 변경 | 멀티 프로바이더 추상화, 캐싱 |

---

## 6. 바로 다음 액션 (Phase 1 착수안 — 하이브리드)

1. **측정 워커**: `분야+지역+로펌명` → 질문셋을 *Gemini 무료 티어/Perplexity*에 호출 → 원시 응답 수집.
2. **로컬 판독기**: 서버의 Gemma 3n E2B가 응답마다 "대상/경쟁사 언급" 분류 → 점유율 JSON 산출(외부 비용 0).
3. **리드 적재**: 사전등록 폼 실연결(Supabase)로 이메일·진단이력 저장.
4. **자가진단 자동화**: `lawgeo.html`의 붙여넣기 단계를 위 측정 워커 결과로 자동 대체.

**필요 자원(최소)**
- Gemini API 키(무료 티어로 시작) — *측정 전용*.
- 로컬 Gemma 구동 서버(중급 GPU 1장 또는 CPU) — *판독·생성·영업문구*.
- 배포/DB: Vercel + Supabase(둘 다 무료 티어 가능).

> 핵심: 시작 단계 외부 비용 ≈ 0. 무료 티어 한도를 넘는 대량화 시점에만 유료 전환.
